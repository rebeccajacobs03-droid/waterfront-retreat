{
  "name": "waterfront-retreat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp *.html *.css *.js .nojekyll public/ && cp -R images public/"
  }
}
