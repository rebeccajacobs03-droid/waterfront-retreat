{
  "name": "waterfront-retreat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html cabin.html cabins.html globals.css gallery.css outdoor-gallery.css app.js public/ && cp -R images public/ && cp *.jpg *.jpeg *.png *.webp public/ || true"
  }
}
